import {
    getCustomAttribute,
    getLocation,
    getDetailUrl,
} from "../api/jobs"

export default {
    methods: {
        getCustomAttribute,
        getLocation,
        getDetailUrl,
    },
}
